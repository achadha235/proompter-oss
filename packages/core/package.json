{
  "name": "@proompter/core",
  "main": "./build/index.js",
  "types": "./index.ts",
  "type": "module",
  "version": "0.0.4",
  "private": false,
  "scripts": {
    "build": "bun build ./index.ts --splitting --target node --sourcemap=external --outdir ./build",
    "dev": "pnpm run build --watch"
  },
  "dependencies": {
    "@proompter/ai": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}