{
  "name": "@proompter/server-next",
  "module": "index.ts",
  "type": "module",
  "dependencies": {
    "ai": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "pnpm run build --watch",
    "build": "bun build ./index.ts --splitting --target node --sourcemap=external --outdir ./build"
  }
}